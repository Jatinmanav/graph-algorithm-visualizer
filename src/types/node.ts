export default interface node {
  count: number;
  clientX: number;
  clientY: number;
  canvasX: number;
  canvasY: number;
  windowX: number;
  windowY: number;
  color: string;
  visualize: boolean;
}

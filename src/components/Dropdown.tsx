import React from "react";
import node from "../types/node";

type AppProps = { nodeList: node[]; setNode(value: number): void };

const Dropdown = ({ nodeList, setNode }: AppProps) => {
  return <div></div>;
};

export default Dropdown;
